'use client';import Image from 'next/image';import Link from 'next/link';import {ShoppingCart,GitCompare,Star} from 'lucide-react';import {Product,money} from '@/lib/data';import {useCart} from './cart-provider';
export default function ProductCard({p}:{p:Product}){const{add}=useCart();const off=Math.round((1-p.salePrice/p.price)*100);return <article className="card group overflow-hidden p-3 relative hover:shadow-xl transition"><span className="absolute z-10 top-3 left-3 rounded-md bg-red-600 text-white px-2 py-1 text-xs font-bold">-{off}%</span><div className="relative aspect-square rounded-xl overflow-hidden bg-gray-50"><Image src={p.image} alt={p.name} fill sizes="(max-width:700px) 50vw, 240px" className="object-cover group-hover:scale-105 transition duration-300"/></div><div className="pt-3"><span className="text-xs font-bold text-[#18181b]">{p.brand} · {p.badge}</span><Link href={'/san-pham/'+p.slug}><h3 className="font-semibold mt-1 min-h-12 line-clamp-2 hover:text-[#18181b]">{p.name}</h3></Link><div className="flex items-center gap-1 text-sm mt-1"><Star size={15} fill="#fbbf24" color="#fbbf24"/><b>{p.rating.toFixed(1)}</b><span className="text-gray-400">({p.reviews})</span></div><del className="text-sm text-gray-400 block mt-2">{money(p.price)}</del><strong className="text-xl text-red-600">{money(p.salePrice)}</strong><div className="grid grid-cols-[1fr_auto] gap-2 mt-3"><button onClick={()=>add(p)} className="btn btn-blue text-sm"><ShoppingCart size={17}/>Thêm giỏ</button><button aria-label="So sánh" className="border rounded-lg px-3 text-[#18181b]"><GitCompare size={18}/></button></div></div></article>}

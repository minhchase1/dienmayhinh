import {products} from '@/lib/data';import ProductCard from '@/components/product-card';export default async function Search({searchParams}:{searchParams:Promise<{q?:string}>}){const q=(await searchParams).q||'';const list=products.filter(p=>(p.name+p.brand).toLowerCase().includes(q.toLowerCase()));return <main className="container py-10"><h1 className="text-3xl font-black">Kết quả tìm kiếm</h1><p className="text-gray-500 mt-2">Tìm thấy {list.length} sản phẩm cho “{q}”</p>{list.length?<div className="grid grid-cols-2 md:grid-cols-4 gap-4 mt-6">{list.map(p=><ProductCard p={p} key={p.id}/>)}</div>:<div className="card p-16 text-center mt-6"><h2 className="text-xl font-bold">Không tìm thấy sản phẩm</h2><p className="text-gray-500 mt-2">Thử từ khóa ngắn hơn hoặc tên thương hiệu.</p></div>}</main>}
